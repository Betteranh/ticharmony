package be.stage.ticharmony.repository;

import be.stage.ticharmony.model.PartnerEntry;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartnerEntryRepository extends JpaRepository<PartnerEntry, Long> {
}